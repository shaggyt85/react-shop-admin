/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ['images.unsplash.com', 'api.lorem.space', 'tailwindui.com'],
  },
}

module.exports = nextConfig
